apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracking-service
  namespace: cognivita
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: /actuator/prometheus
    prometheus.io/port: "8094"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tracking-service
  template:
    metadata:
      labels:
        app: tracking-service
    spec:
      initContainers:
        - name: wait-for-mysql
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z mysql 3306; do sleep 2; done"]
        - name: wait-for-eureka
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z eureka-server 8761; do sleep 2; done"]
      containers:
        - name: tracking-service
          image: medsadek/tracking-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8094
          env:
            - name: SERVER_PORT
              value: "8094"
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://mysql:3306/tracking_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
            - name: SPRING_DATASOURCE_USERNAME
              value: root
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_ROOT_PASSWORD
            - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
              value: http://eureka-server:8761/eureka/
            - name: SPRING_MAIL_USERNAME
              value: ""
            - name: SPRING_MAIL_PASSWORD
              value: ""
---
apiVersion: v1
kind: Service
metadata:
  name: tracking-service
  namespace: cognivita
spec:
  type: ClusterIP
  selector:
    app: tracking-service
  ports:
    - name: http
      port: 8094
      targetPort: 8094
