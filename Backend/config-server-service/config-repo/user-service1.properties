# user-service1 config — served by Config Server.
# Secrets are NOT stored here. They are loaded from the .env file
# directly by the service at startup (spring.config.import in application.properties).
spring.application.name=user-service1
server.port=8081
